@model IEnumerable<ProyectoMarniLopezLopez.Models.pay_master>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pay_dsc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pay_dsc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pay_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.pay_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pay_id })
        </td>
    </tr>
}

</table>
